<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FinSolutions - Professional Financial Management</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#3B82F6',
                        secondary: '#1E40AF',
                        accent: '#10B981'
                    }
                }
            }
        }
    </script>
    <style>
        .hero-section {
            background-image: url('/img/bg.png');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            background-attachment: fixed;
        }
        
        .hero-overlay {
            background: linear-gradient(135deg, rgba(46, 47, 48, 0.144) 0%, rgba(0, 0, 0, 0.9) 100%);
        }
        
        .text-shadow {
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.3);
        }
        
        .button-glow {
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
            transition: all 0.3s ease;
        }
        
        .button-glow:hover {
            box-shadow: 0 0 30px rgba(0, 0, 0, 0.349);
            transform: translateY(-2px);
        }
    </style>
</head>
<body class="m-0 p-0 overflow-hidden">
    <!-- Hero Section -->
    <section class="hero-section h-screen w-full relative flex items-center justify-center">
        <!-- Overlay -->
        <div class="hero-overlay absolute inset-0"></div>
        
        <!-- Content -->
        <div class="relative z-10 text-center px-6 max-w-4xl mx-auto">
            <!-- Main Title -->
            <h1 class="text-6xl md:text-8xl font-bold text-white mb-6 text-shadow">
                FinSolutions
            </h1>
            
            <!-- Subtitle -->
            <p class="text-xl md:text-2xl text-gray-100 mb-12 text-shadow max-w-2xl mx-auto leading-relaxed">
                Professional Financial Management Platform
                <br>
                <span class="text-lg md:text-xl text-blue-200">
                    Take control of your finances with our comprehensive suite of tools
                </span>
            </p>
            
            <!-- Buttons -->
            <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
                <!-- Login Button -->
                <a href="/auth/login" 
                   class="button-glow bg-white text-primary font-bold py-4 px-8 rounded-full text-lg hover:bg-gray-100 transform hover:scale-105 transition-all duration-300 flex items-center justify-center min-w-[200px]">
                    <i class="fas fa-sign-in-alt mr-3"></i>
                    Login
                </a>
                
                <!-- Register Button -->
                <a href="/auth/register" 
                   class="button-glow bg-transparent border-2 border-white text-white font-bold py-4 px-8 rounded-full text-lg hover:bg-white hover:text-primary transform hover:scale-105 transition-all duration-300 flex items-center justify-center min-w-[200px]">
                    <i class="fas fa-user-plus mr-3"></i>
                    Register
                </a>
            </div>
        </div>
        
        <!-- Decorative Elements -->
        <div class="absolute top-10 left-10 w-20 h-20 border-2 border-white opacity-20 rounded-full animate-pulse"></div>
        <div class="absolute bottom-20 right-20 w-16 h-16 border-2 border-white opacity-20 rounded-full animate-pulse delay-1000"></div>
        <div class="absolute top-1/3 right-10 w-12 h-12 border-2 border-white opacity-20 rounded-full animate-pulse delay-500"></div>
    </section>
</body>
</html>
