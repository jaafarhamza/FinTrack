<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Forgot Password - FinTrack</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#3B82F6',
                        secondary: '#1E40AF',
                        accent: '#10B981'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gray-500 min-h-screen flex items-center justify-center p-4">
    <div class="w-full max-w-md">
        <%- include('../partials/header', { subtitle: 'Reset your password' }) %>

        <div class="bg-gray-400 rounded-2xl shadow-xl p-8">
            <h2 class="text-2xl font-semibold text-gray-900 mb-6 text-center">Forgot Password</h2>

            <%- include('../components/messages') %>

            <form action="/auth/forgot-password" method="POST" class="space-y-6">
                <div class="text-center mb-6">
                    <p class="text-gray-700">
                        Enter your email address and we'll send you a link to reset your password.
                    </p>
                </div>

                <%- include('../components/formField', {
                    fieldName: 'email',
                    label: 'Email Address',
                    type: 'email',
                    icon: 'envelope',
                    value: formData.email,
                    placeholder: 'Enter your email address'
                }) %>

                <%- include('../components/submitButton', {
                    icon: 'paper-plane',
                    text: 'Send Reset Link'
                }) %>
            </form>

            <div class="mt-6 text-center space-y-4">
                <div>
                    <a href="/auth/login" class="text-primary hover:text-secondary font-medium transition duration-200">
                        <i class="fas fa-arrow-left mr-1"></i>Back to Login
                    </a>
                </div>
                
                <div>
                    <a href="/home" class="text-gray-500 hover:text-primary text-sm transition duration-200">
                        <i class="fas fa-home mr-1"></i>Back to Home
                    </a>
                </div>
            </div>
        </div>

        <%- include('../partials/footer') %>
    </div>
</body>
</html>
