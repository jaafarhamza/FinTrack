<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - FinTrack</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#3B82F6',
                        secondary: '#1E40AF',
                        accent: '#10B981'
                    }
                }
            }
        }
    </script>
</head>
<body class="bg-gradient-to-br from-blue-50 to-indigo-100 min-h-screen">
    <div class="container mx-auto px-4 py-16">
        <div class="text-center">
            <!-- Logo -->
            <div class="inline-flex items-center justify-center w-20 h-20 bg-blue-600 rounded-full mb-6">
                <i class="fas fa-wallet text-white text-3xl"></i>
            </div>
            
            <h1 class="text-5xl font-bold text-gray-900 mb-4">Welcome to Dashboard</h1>
            <p class="text-xl text-gray-600 mb-8">Hello <%= user.firstName %>! You're successfully logged in.</p>
            
            <div class="bg-white rounded-lg shadow-lg p-6 max-w-md mx-auto mb-8">
                <h2 class="text-2xl font-semibold text-gray-800 mb-4">Your Profile</h2>
                <div class="space-y-2 text-left">
                    <p><strong>Name:</strong> <%= user.firstName %> <%= user.lastName %></p>
                    <p><strong>Email:</strong> <%= user.email %></p>
                    <p><strong>Balance:</strong> $<%= user.balance %></p>
                    <p><strong>Currency:</strong> <%= user.currency %></p>
                </div>
            </div>
            
            <div class="space-y-4">
                <a href="/auth/logout" 
                   class="inline-block bg-red-600 hover:bg-red-700 text-white font-semibold py-3 px-8 rounded-lg transition duration-200 transform hover:scale-105">
                    <i class="fas fa-sign-out-alt mr-2"></i>Logout
                </a>
            </div>
        </div>
    </div>
</body>
</html>
